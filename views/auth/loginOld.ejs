<!--
<% if (errors) {%>
  <% errors.forEach(function (error) { %>
    <%= __(error) %>
  <% }) %>
<% } %>
<form role="form" action="/auth/local" method="post">-->


  <input type="text" name="identifier" placeholder="Имя или email" id="identifier">
  <input type="password" name="password" placeholder="Пароль" id="password">
  <button onclick="login()">Войти</button>


  <!--<button type="submit">Sign in</button>-->
<!--</form>-->

<!--
<% if (Object.keys(providers).length) {%>
  <h4>You can also use one of these...</h4>

  <% Object.keys(providers).forEach(function (key) { %>
    <a href="/auth/<%= providers[key].slug %>" role="button"><%= providers[key].name %></a>
  <% }) %>
<% } %>
-->


<script type="text/javascript">
function login(){
    console.log('LOGIN!');
    var params = {
        'identifier': document.getElementById('identifier').value,
        'password' : document.getElementById('password').value,
        'act': 'login'
    };
//
    io.socket.post('/auth/local', params, function (data, jwres){
        console.log('income:', data);
    });

}

</script>